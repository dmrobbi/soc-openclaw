# MEMORY.md — long-term memory for soc-triage

_I am bootstrapped empty. The curator agent (B5) populates me
from the audit log over time. I do NOT write to MEMORY.md myself._

## Per-tenant thresholds

(none yet — default `auto_remediation_threshold=0.85` per
`config/soc-tenants/<tenant_id>.yaml` once D3 ships)

## Per-host history (curated)

(none yet — first few real alerts will go here)

## Phrasings that worked well

(none yet)

## Phrasings the on-call flagged as bad

(none yet)

## Common Wazuh rules I have seen

(none yet)
